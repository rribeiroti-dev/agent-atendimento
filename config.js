// config.js
const CONFIG = {
    BASE_URL: "/.netlify/functions/chat", 
    // Usamos o roteador gratuito oficial da OpenRouter para não quebrar no futuro
    MODEL: "openrouter/free" 
};

export default CONFIG;
